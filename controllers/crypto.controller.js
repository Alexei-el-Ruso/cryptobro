import {getSHA256Hash} from "boring-webcrypto-sha256";

const lockData = async (req, res) => {
    try {
        const clave =
        const data = req.body;

        const cryptedData = crypto.AES.encrypt(data);
    }
}

module.exports = {

}